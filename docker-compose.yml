services:
  claudegate:
    build: .
    container_name: claudegate
    restart: unless-stopped
    ports:
      - "8082:8082"
    env_file:
      - .env
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8082/health"]
      interval: 10s
      timeout: 3s
      retries: 3
